import React, { Component } from 'react';
class DeleteComponents extends Component {
    constructor(props) {
        super(props);
        this.state = {  };
    }

    
    render() {
        return (
            <button>
                Delete
            </button>
        );
    }
}

export default DeleteComponents;